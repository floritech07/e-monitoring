apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: nexusmonitor-api
  namespace: nexusmonitor
  labels:
    app: api
    release: prometheus
spec:
  selector:
    matchLabels:
      app: api
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
    scheme: http
